<h2>to <%= @whom.name %></h2>

<%= form_with data: {chat_id: Chat.key_pair(current_user, @whom).join('_') } do |f| %>
  <%= f.text_field :text, autofocus: true %>
<% end %>

<div class="log">
  <% 10.times do |msg| %> <%# todo %>
    <%= render 'chats/msg', msg: msg %>
  <% end %>
</div>
