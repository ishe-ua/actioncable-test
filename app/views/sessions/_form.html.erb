<%= form_with url: sessions_path do |f| %>
  <div class="alert"><%= flash.alert %></div>
  <div class="field">
    <%= f.label 'Name:' %>
    <%= f.text_field :name, autofocus: true %>
  </div>

  <%= f.submit 'OK' %>
<% end %>
